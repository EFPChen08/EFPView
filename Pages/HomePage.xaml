<Page
    x:Class="EFPView.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EFPView.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">

    <ScrollViewer
        x:Name="PageScroll"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Enabled"
        HorizontalScrollBarVisibility="Disabled"
        ZoomMode="Disabled"
        Padding="0">
        <Grid
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Image Source="/Assets/winuiview.png"
                   Width="120" Height="120"
                   Stretch="Uniform"
                   Opacity="0.9"/>
            </Grid>
            <Grid Grid.Row="1">
                <TextBlock Text="主页 -- WinUI2.8 UWP" TextAlignment="Center" Margin="24" FontSize="24"/>
            </Grid>
            <Grid Grid.Row="2">
                <TextBlock Text="UI基于WInUI2.8 Fluent设计" TextAlignment="Center" Margin="24,0,0,0" FontSize="12"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
